[project]
name = "image-scripts"
version = "0.1.0"
description = "Image processing and generation scripts"
requires-python = ">=3.8"
dependencies = [
    "rembg>=2.0.60",
    "pillow>=10.0.0",
    "zai-sdk>=0.2.1",
]

[tool.hatch.build.targets.wheel]
packages = []
